import { LayoutRules } from '../types';

export const treemapRules: LayoutRules = {
    idealAspectRatio: 1.5,
    preferredOrientation: 'landscape',
    legendPosition: 'bottom',
    marginPriority: ['bottom'],
    minPlotWidth: 0.5
};
